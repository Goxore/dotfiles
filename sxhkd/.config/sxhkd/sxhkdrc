# scripts
super + a
	bash $HOME/scripts/fastaccess.sh

super + shift + c
	bash $HOME/scripts/editconfig.sh

super + s
	bash $HOME/scripts/sb -r

super + v
	bash $HOME/scripts/mictoggle.sh

super + ctrl + c
	rofi -modi 'clipboard:greenclip print' -show clipboard -run-command '{cmd}'

#music
super + plus
	mpc volume +5

super + minus
	mpc volume -5

super + p
	mpc toggle

super + bracketright
	mpc next

super + bracketleft
	mpc prev

super + u
	rofi-rbw
super + Delete
	$HOME/scripts/holdwork.sh

super + Insert
	$HOME/scripts/continuework.sh
